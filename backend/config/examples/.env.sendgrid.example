## Archived SendGrid example removed.
## SendGrid support was removed from the codebase. If you need an HTTP-based
## provider in future, add your own `backend/config/.env` with the provider's
## credentials (e.g. Mailgun, Postmark).
